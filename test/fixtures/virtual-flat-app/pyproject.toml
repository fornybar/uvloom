[project]
name = "virtual-flat-app"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "colorama>=0.4.6",
]

[dependency-groups]
dev = ["typing-extensions"]

[tool.uv]
package = false
