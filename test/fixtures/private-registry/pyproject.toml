[project]
name = "private-registry-app"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["private-wheel"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[[tool.uv.index]]
name = "private"
url = "https://private.example/simple/"
authenticate = "always"
