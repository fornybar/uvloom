[project]
name = "string-license-files"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []
license-files = "LICENSE*"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/string_license_files"]
