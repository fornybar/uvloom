[project]
name = "group-package"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = []

[dependency-groups]
test = ["typing-extensions"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
