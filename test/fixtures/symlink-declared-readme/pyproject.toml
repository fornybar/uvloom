[project]
name = "flat-pkg"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []
readme = "README.md"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["flat_pkg"]

readme = "README.md"
