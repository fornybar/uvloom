[project]
name = "no-match-license-glob"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []
license-files = ["NOTICES*"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["no_match_license_glob"]
