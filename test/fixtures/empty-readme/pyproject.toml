[project]
name = "empty-readme"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []
readme = ""

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["empty_readme"]
