[project]
name = "bad-license-glob"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []
license-files = ["LICEN{CS}E*"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
